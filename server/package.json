{
  "name": "server",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.0",
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "drizzle-kit": "^0.30.4"
  }
}
